embedding_size: 64
feat_embed_dim: 64

n_layers: [2] # [1, 2] s
dropout: [0.3] # [0.3, 0.5]
reg_weight: [0.01]
cl_weight: 2.0

use_neg_sampling: False



# Missing Modality Setting
missing_modal : 0
missing_imputation : [0] # [0, 1, 2]

new_items : 0 #1


hyper_parameters: ["n_layers", "reg_weight", "dropout",  "missing_imputation"]

